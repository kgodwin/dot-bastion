"syntax enable		" syntax highlight / commented out to fix issue with crontab -e
set nonumber 	    " line numbers are bad
colorscheme janah	" my taste in vim color
set visualbell		" no noise, I prefer gregorian chant, rap battle, or c-span
set autoread		" reload foreign changes
set showmode		" we want the mode
set showcmd	    	" we want incomplete cmds
set ruler           " cursor position rules
set showmatch       " bracket matching ftw
set nowrap          " nowrap; no lines over 120 chars folks

"4 space ident
set tabstop=4
set shiftwidth=4
set softtabstop=4
set smarttab
set expandtab
